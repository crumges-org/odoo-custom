<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_employee_form_inherit_portal" model="ir.ui.view">
        <field name="name">hr.employee.form.inherit.portal</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <!-- Allow selecting any user (including Portal) in Related User field -->
            <field name="user_id" position="replace">
                <field name="user_id" string="Related User" 
                       domain="[('company_ids', 'in', company_id)]" 
                       context="{'default_create_employee_id': id}" 
                       widget="many2one_avatar_user"/>
            </field>
        </field>
    </record>
</odoo>
